{
  "name": "tenant-worker",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/server/tenant/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun build --minify apps/server/tenant/src/index.ts --outfile dist/build/tenant/worker.ts"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wrangler deploy",
        "args": ["--config=./apps/server/tenant/wrangler.toml"]
      }
    },
    "wrangler": {
      "command": "wrangler dev",
      "options": {
        "args": ["--config=./apps/server/tenant/wrangler.toml", "--port=3000"]
      },
      "configurations": {
        "remote": {
          "args": ["--config=./apps/server/tenant/wrangler.dev.toml", "--port=3002", "--remote"]
        },
        "local": {
          "args": ["--config=./apps/server/tenant/wrangler.dev.toml", "--port=3002"]
        }
      },
      "defaultConfiguration": "development"
    }
  },
  "tags": ["type:app", "scope:worker", "provider:cloudflare", "scope:tenant", "scope:auth"],
  "implicitDependencies": ["@snappbiz/auth"]
}
